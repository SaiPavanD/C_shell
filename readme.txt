Compile shell.c in /src with -lreadline flag.
              (OR)
Run make to build and execute the shell.

Built-in commands : cd, lsb, exit.

Output of background processes will be written to background_processes_output.txt by default.
I/O redirection and piping has been implemented.
